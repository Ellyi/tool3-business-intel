[phases.setup]
nixPkgs = ['python311', 'postgresql']

[phases.install]
cmds = ['pip install -r requirements.txt --break-system-packages']

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120'
```

---

# FILE 8: .gitignore

**Location:** `tool3-business-intel/backend/`  
**Path:** `tool3-business-intel/backend/.gitignore`
```
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
*.pdf
